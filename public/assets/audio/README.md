Drop your original / royalty-free ambient lo-fi cinematic loop here as `audio.mp3`
(i.e. `public/assets/audio/audio.mp3`). The page plays it automatically on load,
looping, with no mute/unmute control — if your browser blocks autoplay with sound,
it silently retries on the visitor's first click or keypress, per browser policy.
